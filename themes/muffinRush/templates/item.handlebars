<div class="visual">
    <img src="{{itemBackground}}" class="background">
    <img src="{{imgFilePath}}" class="foreground">
    <div class="balance">
        <label>{{balance}}</label>
    </div>
</div>
<div class="content">
    <h1>{{name}}</h1>
    <div class="description">{{description}}</div>
</div>
<div class="price">
    <img src="{{currency.imgFilePath}}" class="currency">
    <label>{{price}}</label>
</div>
